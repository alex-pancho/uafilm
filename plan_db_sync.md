# ПЛАН СИНХРОНІЗАЦІЇ БАЗИ ДАНИХ ЧЕРЕЗ GIT

Чи можна заставити Flask запускати і синкати гіт?

Можна 😄 І це цілком робоча схема:
**Flask → перед стартом робить `git pull` → застосовує міграції → запускається.**

Тобто:

> Git = транспорт
> SQLite + migrations = дані
> Flask = мозок

---

## 🧩 ВАРІАНТИ

### ✅ 1. Flask робить `git pull` при старті

```python
import subprocess

def git_sync():
    try:
        subprocess.run(["git", "pull"], check=True)
        print("Git synced")
    except Exception as e:
        print("Git sync failed:", e)
```

і в `app.py` перед запуском:

```python
git_sync()
apply_migrations()
```

Тоді:

* ти пушиш `migrations/*.sql`
* інший ПК запускає exe
* він робить `git pull`
* бачить нові `.sql`
* застосовує

---

## ⚠️ АЛЕ ВАЖЛИВО

### ❌ НЕ синкай через git сам `films.sqlite`

тільки:

```
migrations/
templates/
static/
код
```

бо SQLite через git = 💥 конфлікти

---

## 🔐 АВТОРИЗАЦІЯ

Щоб не вводити пароль кожен раз:

### варіант 1 — SSH key (правильний)

```bash
ssh-keygen
```

і додати ключ у GitHub/GitLab

### варіант 2 — токен (гірше)

в remote URL з токеном

---

## 🧠 ЛОГІКА ЗАПУСКУ

```text
exe стартує
  ↓
git pull
  ↓
apply_migrations()
  ↓
Flask server
```

---

## 🔁 МОЖНА Й PUSH ЗРОБИТИ

Наприклад, якщо Scrapy згенерив новий `005.sql`:

```python
subprocess.run(["git", "add", "migrations"])
subprocess.run(["git", "commit", "-m", "new data"])
subprocess.run(["git", "push"])
```

але ⚠️ це вже небезпечно без контролю (конфлікти)

---

## 🧨 ПРОБЛЕМА КОНФЛІКТІВ

Якщо два ПК одночасно:

* створили `006.sql`
  → конфлікт і все

Рішення:

* іменувати так:

```
2026-02-08_16-30.sql
```

або

```
pc1_012.sql
pc2_045.sql
```

---

## 🧪 БІЛЬШ БЕЗПЕЧНИЙ ВАРІАНТ

Git тільки:
✅ для доставки даних
❌ не для двосторонньої синхронізації

А для P2P:
👉 Syncthing + migrations
(без git)

---

## 🎯 КОЛИ GIT = ОК

Git ок якщо:
✔ ти один автор
✔ або read-only клієнти
✔ або централізоване джерело
✔ або просто апдейти БД

Git погано якщо:
❌ 10 ПК щось пишуть одночасно

---

## 💡 КРУТИЙ ГІБРИД

```
Git = код + migrations
Syncthing = локальні медіа
SQLite = локальна БД
```

---

## ❓Хочеш — можу дати готову функцію:

✅ `git_sync()` + `apply_migrations()` разом
✅ з логами
✅ з Windows-safe
✅ з перевіркою, що git є

Скажи, який варіант хочеш:

👉 **1 — тільки git pull при старті**
👉 **2 — pull + migrations (готовий код)**
👉 **3 — push із Scrapy**
👉 **4 — схема під exe (PyInstaller)**

і я зразу дам тобі робочий приклад під твій Flask.
